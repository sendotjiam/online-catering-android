<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sendo.onlinecatering">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineCatering">
        <activity android:name=".AllMenuPage"></activity>
        <activity android:name=".ChatwithCustomer" />
        <activity android:name=".OrderDetail" />
        <activity android:name=".ChatAdmin" />
        <activity android:name=".CustomerOrder" />
        <activity android:name=".CheckOutPage" />
        <activity android:name=".InvoicePage" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>